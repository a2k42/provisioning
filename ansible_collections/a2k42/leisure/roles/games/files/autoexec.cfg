bind "h"            "hud_toggle_floating"       // unassigned / unassigned
bind "k"            "inventory_resupply"        // voicerecord / unassigned
bind "v"            "+duck"                     // unassigned / config and config_default differ
bind "MOUSE4"       "+special2"
bind "MOUSE5"       "+voicerecord"              // unassigned / "k"

con_enable "1"
sensitivity "4.800000"
cl_zoom_sensitivity_scope "2.500000"
snd_radio_filter "0"
voice_scale "3.000000"
volume "0.050000"

// Overwrite conflicting config.cfg vars with these - MUST BE AT END OF CVARS OF AUTOEXEC.CFG
host_writeconfig
